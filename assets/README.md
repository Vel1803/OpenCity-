# Assets

Shared visual assets for OpenCity.

- `logo/` — project logo files
- `screenshots/` — screenshots of the working prototype/dashboards
- `icons/` — UI icons
- `diagrams/` — exported diagram images (mirrors of `architecture/`, used in docs/README)
- `ppt/` — images used in the pitch deck
